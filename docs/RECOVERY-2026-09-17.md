# คืนฐานเกมกบแคนนอน — 17 กันยายน 2026

เจ้าของยืนยันว่าฐานล่าสุดที่ยอมรับคือเกมหลังเปลี่ยน Thorn ปกติเป็น Moss Frog พร้อมอนิเมชันครบ งานช้างและการซ่อม prototype หลังจากนั้นยังไม่ใช่แคนนอน

## ฐานเดียว
`game/` อ้าง tree `8290f60b6708eae7689ded9568264b70d5363eda` โดยตรงจาก commit `cf841e09ad37da59a8edb1911200d6a41979c5cc` ไม่ใช่การประกอบโค้ด/ภาพกลับทีละไฟล์ และไม่ได้เอา root Vite รุ่นเก่ามา build ใหม่ ทั้งชุดมี 1,311 ไฟล์ รวม 181,024,551 bytes

นี่คือชุดเกมพร้อมเล่นที่เก็บไว้อย่างครบถ้วน ไม่ใช่คำกล่าวว่ากู้ authoring source ที่ตรงกับทุกฟีเจอร์ได้แล้ว การแยก source เพื่อแก้เกมต่อเป็นคนละงานกับการคืนฐานนี้

## สำรองก่อนเคลียร์
สำรอง main ที่ `543bd352` ไว้ที่ `archive/before-frog-canon-cleanup-20260917` แล้ว โค้ดเก่าและ Water Calf ยังอยู่ในสายสำรอง ประวัติ Git ไม่ถูกเขียนทับ งานทดลอง branch อื่นไม่ถูกลบ แต่ไม่ถูกนำมาใช้ใน build ปัจจุบัน

## หลักฐานรอบแรก
GitHub Actions run `35219187347` ของ commit `41a8f5a011c3bb7e38dade1bfb27ae935b6074d1`:
- ตรวจ byte tree และคัดลอก game ไป dist โดยไม่เปลี่ยนเกม: ผ่าน
- Chromium และ WebKit เปิดฉาก เลือกการ์ด เกิดกบ เดินเฟรมจริง พร้อมภาพหน้าจอ: ผ่าน
- ตรวจ hop/rest, facing สองด้าน, hit, tongue และ death จาก renderer ของเกม: ผ่าน
- เปิดภาพหญ้า/ต้นไม้/บึง/กบ/การ์ดด้วย image decoder จริง: ผ่าน
- เปรียบเทียบ SHA-256 ของไฟล์บน `https://slime-evolution-five.vercel.app/` กับแคนนอน: ตรง 1,311 จาก 1,311 ไฟล์ ไม่มีไฟล์ขาดหรือ byte ต่าง

ดังนั้นเว็บเดิมยังเป็นชุดกบที่ครบอยู่แล้ว ลิงก์ Water Calf test ที่เสียเป็นคนละลิงก์ ไม่จำเป็นต้องเผยแพร่ prototype ใหม่ทับเว็บเดิมเพื่อคืนกบ ห้ามอ้างว่ารอบนี้ deploy ใหม่ถ้าไม่ได้ทำจริง

CI รุ่นปัจจุบันเพิ่มการเปิดโดเมนเดิมเล่นจริงโดยตรงทั้ง Chromium และ WebKit นอกเหนือจากการทดสอบ dist และเทียบทุกไฟล์ ผลแต่ละรอบให้อ่านจาก Actions และ artifact จริง ห้ามอ้างค่า FPS ใน software-rendered CI ว่าเป็น FPS ของ iPad
