name: HUD polish
on:
  pull_request:
    paths:
      - 'ui/hud-polish.mjs'
      - 'scripts/canon.mjs'
      - 'CANON.json'
      - 'tests/hud-polish.test.mjs'
      - 'tests/hud-polish-browser.mjs'
      - '.github/workflows/hud-polish.yml'
permissions:
  contents: read
jobs:
  hud-polish:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      LIBGL_ALWAYS_SOFTWARE: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm test && npm run build
      - run: |
          npm install --no-save --package-lock=false playwright@1.62.0
          npx playwright install --with-deps chromium webkit
      - run: |
          node scripts/canon.mjs serve >/tmp/hud-server.log 2>&1 &
          SERVER_PID=$!
          trap 'kill $SERVER_PID || true' EXIT
          for i in {1..30}; do curl -fsS http://127.0.0.1:4173/release.json >/dev/null && break || sleep 1; done
          xvfb-run -a node tests/hud-polish-browser.mjs && BROWSER=webkit xvfb-run -a node tests/hud-polish-browser.mjs
