# Owner-approved Pond Turtle sources

- `approved-walk.mp4`: original user-uploaded H3 video, preserved byte-for-byte; not a generated replacement sheet.
- `approved-poses-upload.jpg`: final user reference upload, preserved byte-for-byte.
- `approved-poses.png`: the corresponding lossless original generated earlier in the conversation and then approved by the user; original hurt, shell-only death and three-quarter shield poses.

Runtime preparation: `tools/prepare-turtle-assets.py`. All source hashes, selected frame indices/timestamps, transforms and atlas cell meanings: `game/assets/enemies/pond-turtle-atlas.json`. The source video is archival input only, never played/decoded during gameplay.
