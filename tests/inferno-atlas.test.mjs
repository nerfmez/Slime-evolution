import test from 'node:test';import assert from 'node:assert/strict';
import {atlasState} from '../vfx/inferno-atlas.js';import {normalizePreset} from '../vfx/inferno-settings.js';import {atlasAnchors,atlasSizes} from '../vfx/inferno-atlas-data.js';
test('atlas playback terminates and stays addressable with short and long imported presets',()=>{for(const raw of [{},{pop:.08,hold:.02,breakTime:.12,duration:1.1},{pop:.4,hold:.4,breakTime:.65,duration:3,curve:5}]){const s=normalizePreset(raw);let previous=0;for(let age=0;age<s.duration+.2;age+=1/120){const a=atlasState(age,s);assert.ok(a.index>=previous);previous=a.index;assert.ok(atlasAnchors[a.index]>0&&atlasAnchors[a.next]<1);assert.ok(atlasSizes[a.index].every(n=>n>0));assert.ok(a.mix>=0&&a.mix<=1&&a.opacity>=0&&a.opacity<=1);}assert.equal(atlasState(s.duration+.01,s).opacity,0);}});
