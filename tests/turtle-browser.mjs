// Run the existing Turtle suite, then the new Panda suite on the same configured site.
// Existing workflow definitions and permissions remain unchanged.
await import('./turtle-browser-core.mjs');
await import('./panda-browser.mjs');
